en:
  settings:
    index:
      title: Settings
      default_value: "Default value: %{value}"
      plugins:
        home_page:
          index:
            title: Core
          tabs:
            general:
              title: General
            apis:
              title: APIs
    
    updates:
      done: Update done. 
            
    vars:
      home_page:
        general:
          navigation:
            items: Navigation Items
          
          enabled_apis: Enabled APIs